import {Parser} from '../../../../src/parser';

describe('.parse() lookup-reference formulas', () => {
  let parser;

  beforeEach(() => {
    parser = new Parser();
  });
  afterEach(() => {
    parser = null;
  });

  it('MATCH', () => {});
});
